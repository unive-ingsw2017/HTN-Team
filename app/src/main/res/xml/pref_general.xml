<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <Preference
        android:summary="@string/version_name"
        android:title="@string/settings_general_app_version" />

    <Preference android:title="@string/settings_general_policy">
        <intent
            android:action="android.intent.action.VIEW"
            android:data="@string/url_privacy" />
    </Preference>

    <Preference android:title="@string/settings_general_privacy">
        <intent
            android:action="android.intent.action.VIEW"
            android:data="@string/url_terms" />
    </Preference>
</PreferenceScreen>
